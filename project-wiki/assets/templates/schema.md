# Wiki schema

Page types and directories are configured in config.json. Customize definitions, aliases and naming for this project's questions before ingesting.

Pages have stable IDs, source fingerprints and directed relations. Keep original sources in place and cite them with relative Markdown links. Separate knowledge status from source freshness. Use local templates for JSON frontmatter.
